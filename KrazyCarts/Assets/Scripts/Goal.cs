using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class Goal : MonoBehaviour
{
    // This script is for the goal when carts are collected
    // After the player hits the collider, the carts should be spawned into the goal area
    // The # of carts collected stays the same, but when it = the amount needed,
    // the level over screen should pop up
    
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
